{
  "name": "@ai-survival/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "pnpm --filter @ai-survival/shared build && vite build",
    "preview": "vite preview",
    "check": "pnpm --filter @ai-survival/shared build && tsc --noEmit",
    "typecheck": "pnpm --filter @ai-survival/shared build && tsc --noEmit"
  },
  "dependencies": {
    "@ai-survival/shared": "workspace:*",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0",
    "typescript": "^5.5.4"
  },
  "devDependencies": {}
}
